# 小程序环境配置

小程序端配置入口：`src/config/env.ts`。

## 当前规则

- `develop`：本地开发环境，默认 `http://127.0.0.1:5173`
- `trial`：体验版环境，等待替换为 HTTPS 后端域名
- `release`：正式版环境，等待替换为 HTTPS 后端域名

## 注意事项

- 不要把密钥写入小程序端代码。
- 微信小程序正式请求必须使用已配置的 HTTPS 合法请求域名。
- 本地调试如果请求本机服务，需要在微信开发者工具里临时关闭合法域名校验，或使用已配置的测试 HTTPS 域名。
